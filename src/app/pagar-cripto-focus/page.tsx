import PagarCriptoFocusClient from "./PagarCriptoFocusClient";

export const metadata = { title: "Pagar Focus con cripto — Focus" };

export default function PagarCriptoFocusPage() {
  return <PagarCriptoFocusClient />;
}
